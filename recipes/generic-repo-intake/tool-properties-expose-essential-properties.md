# tool-properties-expose-essential-properties

Status: generated-only typed contract and advisory recipe are prepared.

The current policy supports inspecting `layer.essentialProperty` through `get_layer_essential_properties` and applying a reviewed `set_expression` only to an explicit generated Essential Property path. Broad Essential Properties traversal, inferred selected-property writes, user precomp template mutation, and MOGRT export remain fail-closed.

Completion remains approval-gated until a generated-only mutating live CEP proof is explicitly allowed.
