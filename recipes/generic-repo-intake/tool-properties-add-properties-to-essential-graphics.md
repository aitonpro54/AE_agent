# tool-properties-add-properties-to-essential-graphics

Status: generated-only typed contract and live-lane metadata are prepared.

The current policy supports adding one explicit generated layer property to Essential Graphics through `add_property_to_essential_graphics`, with controller read-back from `get_essential_graphics_controllers`. Source-exact selected property traversal, user-template mutation, MOGRT export, and controller management semantics remain fail-closed.

Completion remains approval-gated until a generated-only mutating live CEP proof is explicitly allowed.
