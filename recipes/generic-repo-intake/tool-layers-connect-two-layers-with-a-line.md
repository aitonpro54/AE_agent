# tool-layers-connect-two-layers-with-a-line Intake Note

## Source

- Repository: `https://github.com/kyletmartinez/after-effects-scripts`
- Source path: `Layers/Connect_Two_Layers_With_A_Line.jsx`
- Source SHA-256: `85b5c6117977acb2c867cc9cf7f855fd525a5691da6cf0729fcb6efa76732a30`

## Adaptation

This intake adapts only the safe generated dynamic connector-line intent into `recipes/connect-two-layers-with-a-line-typed-plan.md`. No raw JSX is copied into the product.

The supported path creates one generated locked shape layer through `create_layer_connection_line`, using two explicit endpoint layer indices plus expected-name guards. The connector contains one open two-point shape path whose expression is generated by the typed tool and bound only to the two inspected endpoint layer names.

## Fail-Closed Scope

- Source-exact broad selected-layer traversal remains out of scope; selection must be reduced to exactly two reviewed layer indices before mutation.
- Thin rectangle/static guide substitution remains forbidden because it does not preserve dynamic anchor-point connector semantics.
- Generic shape path authoring, arbitrary expression text, endpoint layer mutation, layer stack reordering, selection persistence, and shape contents beyond the generated connector path remain fail-closed.
- Existing layer reorder, rename, relink, timing, parent, track matte, effect/property, keyframe, mask, source, render queue, project item, file, preference, UI state, and non-generated user-asset mutations remain fail-closed.
- Raw ExtendScript and source-checkout execution remain forbidden.

## Validation

- Parent-owned non-live validation covers the bridge contract, semantic verification, scenario fixture/report smoke, solution registry/retrieval, and Full Intaker retry.
- Generated-only live proof uses `node scripts/cep-panel-cdp-smoke.js full-ui-agent-layer-connection-line-openai-cli-smoke` after read-only CEP readiness.
