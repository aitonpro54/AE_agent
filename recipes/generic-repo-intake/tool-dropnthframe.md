# tool-dropnthframe Intake Note

## Source

- Repository: `https://github.com/ae-scripting/scripting-snippets`
- Source path: `dropNthFrame.jsx`
- License: CC-BY-3.0 attribution required, recorded by the auto-intake ledger.

## Adaptation

This intake adapts only the dropped-frame workflow idea into `recipes/dropnthframe-typed-plan.md`. No raw JSX is copied into the product.

The safe supported path is a visual typed-tool adaptation: inspect the target comp, add one generated adjustment layer, apply `ADBE Posterize Time`, set a reviewed frame-rate value when typed effect-property evidence is available, and read the generated layer/effect back.

## Fail-Closed Scope

- Exact source `dropNthFrame.jsx` behavior is not reproduced.
- Real deletion of every Nth timeline frame, irregular frame sampling, layer splitting, time remapping, comp duration changes, keyframe shifts, source replacement, render-output frame dropping, raw ExtendScript, and unrelated layer/property mutation require separate typed-tool contracts.
- The detached child worktree did not contain the source JSX, so this recipe keeps the imported behavior limited to the candidate name, wrapper scope, existing Posterize Time typed tools, and fail-closed generic dropped-frame semantics.

## Validation

- Not run in this child batch by design.
- Parent importer owns registry validation, solution-library validation, and any live acceptance lane.
