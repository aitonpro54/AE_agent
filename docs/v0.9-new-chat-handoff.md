# AE MCP Bridge Handoff - v0.9 Effect Tools

Date: 2026-05-05

This handoff is for starting a fresh Codex chat and continuing work on the AE MCP bridge.

## User Intent

The user wants to continue evolving the AE MCP bridge. Previous work reached stable tagged checkpoints v0.7 and v0.8. The current in-progress work is v0.9 effect tools.

Respond to the user in Russian. They prefer direct execution after a short plan.

## Important Paths

Repo:

`C:\Users\Ant\Documents\Codex\2026-05-03\files-mentioned-by-the-user-c36e102f\ae-mcp-bridge`

Current projectless scratch cwd:

`C:\Users\Ant\Documents\Codex\2026-05-05\ae-mcp-bridge-handoff-c-users`

Current scratch file left from the v0.9 patch run:

`C:\Users\Ant\Documents\Codex\2026-05-05\ae-mcp-bridge-handoff-c-users\apply-v09-effect-tools.js`

It is no longer needed if the repo diff is still present.

## Permissions

The actual repo is outside the writable root for this projectless thread. Reading is fine, but writing to the repo, committing, tagging, creating snapshots, or editing repo docs requires escalated shell approval.

Use `apply_patch` for manual edits inside the writable root. For the actual repo, previous edits were applied by creating patch-runner JS files in the writable cwd, then running them against the real repo with escalated Node.

## Current Git State

HEAD:

`201313e`

Tag on HEAD:

`v0.8.0-property-values`

Current v0.9 worktree is uncommitted. `git status --short` showed:

```text
 M README.md
 M RELEASES.md
 M mcp-server/bridge-daemon.js
 M mcp-server/mcp-adapter.js
 M scripts/bridge-only-smoke-test.js
 M scripts/smoke-test.js
```

`git diff --stat` showed:

```text
 README.md                         |   2 +
 RELEASES.md                       |  20 +++
 mcp-server/bridge-daemon.js       | 289 +++++++++++++++++++++++++++++++++++++-
 mcp-server/mcp-adapter.js         |   2 +-
 scripts/bridge-only-smoke-test.js |   2 +-
 scripts/smoke-test.js             |   2 +-
 6 files changed, 313 insertions(+), 4 deletions(-)
```

There are CRLF warnings in git output, but earlier checks did not show functional errors.

## Stable Completed Checkpoints

v0.7 practical tools:

- Commit: `d19e387` - `Add v0.7 practical AE layer tools`
- Tag: `v0.7.0-practical-layers`
- Snapshot: `snapshots\ae-mcp-bridge-v0.7.0-practical-layers-2026-05-05-184449.zip`
- Added practical tools such as `import_footage`, solid/null/adjustment layer creation, project item placement, comp duplication, and project item search.
- Live AE validation passed and cleaned up temporary project items.

v0.8 property setter:

- Commit: `201313e` - `Add property value setter tool`
- Tag: `v0.8.0-property-values`
- Snapshot: `snapshots\ae-mcp-bridge-v0.8.0-property-values-2026-05-05-184952.zip`
- Added `set_property_value`.
- Live AE validation set Position, Opacity, and TextDocument fields successfully, then cleaned up.

## Current v0.9 Changes Already Applied

Version bumped to `0.9.0` in daemon, adapter, and smoke tests.

README now lists:

- `add_effect`
- `set_effect_property`

RELEASES now contains a `v0.9.0-effect-tools - 2026-05-05` entry with verification notes.

`mcp-server/bridge-daemon.js` gained effect helpers:

- `__codexResolveEffect(layer, effectIndex, effectName, effectMatchName)`
- `__codexEffectProperties(effect, layer, includeValues)`
- `__codexResolveEffectProperty(effect, propertyPath, propertyIndex, propertyName, propertyMatchName)`

New tools:

- `add_effect`
  - Args: `compItemIndex`, `compName`, `layerIndex`, `effect`, optional `name`
  - Adds an AE effect by match/display name, optionally renames it, and returns effect metadata/properties.
- `set_effect_property`
  - Args: `compItemIndex`, `compName`, `layerIndex`, effect selector (`effectIndex` / `effectName` / `effectMatchName`), property selector (`propertyPath` / `propertyIndex` / `propertyName` / `propertyMatchName`), `value`, optional `time`, `setAtTime`
  - Sets an effect property directly or at time and returns before/after metadata.

Tool list is now 32 tools in adapter smoke tests.

## Verification Already Completed for v0.9

Local syntax checks passed:

- `mcp-server/bridge-daemon.js`
- `mcp-server/mcp-adapter.js`

Smoke tests passed:

- daemon-only smoke: health version `0.9.0`
- adapter smoke: listed 32 tools, including `add_effect`, `set_effect_property`, and `set_property_value`

Daemon restarted successfully on port `3456`:

- version: `0.9.0`
- `panelConnected: true`
- pending/inflight commands: none

Live AE validation passed:

- Created temp comp `Codex Test v0.9 Effect Tools`
- Created text layer `Codex Test v0.9 Text`
- `add_effect` added `ADBE Fill`, renamed it `Codex Test v0.9 Fill`
- `set_effect_property` set Fill Color by `propertyIndex: 3`
- `get_layer_details` saw one effect with matchName `ADBE Fill`
- cleanup removed temp comp/items

Final AE project snapshot after cleanup:

- project item count: `10`
- active comp: `titr`
- `Solids` folder exists and has `numItems: 0`
- no leftover Codex test comp/items

## Immediate Next Steps

The new chat should not restart implementation from scratch. Start by confirming the worktree still has the v0.9 diff.

Suggested sequence:

1. Check repo status and bridge status.
2. Run `git diff --check` in the repo. CRLF warnings are acceptable, but whitespace errors should be fixed.
3. If status matches this handoff, commit v0.9:

```powershell
git add README.md RELEASES.md mcp-server/bridge-daemon.js mcp-server/mcp-adapter.js scripts/bridge-only-smoke-test.js scripts/smoke-test.js
git commit -m "Add effect tools"
git tag v0.9.0-effect-tools
```

4. Create snapshot from committed HEAD:

```powershell
$stamp = Get-Date -Format 'yyyy-MM-dd-HHmmss'
$snapshot = "snapshots\ae-mcp-bridge-v0.9.0-effect-tools-$stamp.zip"
git archive --format=zip --output=$snapshot HEAD
Write-Output $snapshot
```

5. Verify:

```powershell
git status --short
git rev-parse --short HEAD
git tag --points-at HEAD
```

6. Remove the scratch patch-runner from the projectless cwd if it still exists:

```powershell
Remove-Item -LiteralPath "C:\Users\Ant\Documents\Codex\2026-05-05\ae-mcp-bridge-handoff-c-users\apply-v09-effect-tools.js"
```

Because the repo is outside the writable root, committing/tagging/snapshot/removing outside writable scope may require escalated approval depending on the tool context. The scratch file is inside the writable root.

## Caveats

The current MCP tool namespace in a running chat may not show newly added tools until the MCP tool list is refreshed or a new chat/server load happens. Manual validation used the project scripts / MCP adapter calls, not the static tool namespace in the chat.

PowerShell displayed one decimal-array value from the Fill Color set operation oddly due locale/formatting, but the AE call succeeded and inspection confirmed the effect existed.

Do not rerun the old patch-runner unless the v0.9 diff is missing. It was already applied.

## Suggested Opening Prompt for New Chat

User can paste:

```text
Продолжаем AE MCP bridge. Прочитай handoff:
C:\Users\Ant\Documents\Codex\2026-05-05\ae-mcp-bridge-handoff-c-users\v0.9-new-chat-handoff.md

Сделай следующие шаги: проверь v0.9 diff, зафиксируй commit/tag/snapshot, убери scratch-файл, и дай короткий отчет.
```
