{
  "schema": "codex-autonomy.validation-lane.v1",
  "id": "scripts-bridge-only-smoke-test",
  "candidate_id": "scripts/bridge-only-smoke-test.js",
  "candidate_path": "scripts/bridge-only-smoke-test.js",
  "created_at": "2026-06-04T13:59:22.920Z",
  "updated_at": "2026-06-04T13:59:22.920Z",
  "type": "explicit-safe-validation-lane",
  "description": "Read-only fixture lane for bridge smoke script static validation; does not spawn the daemon or call localhost.",
  "external_risk_coverage": {
    "approved": true,
    "strategy": "read-only-fixture",
    "evidence": [
      "candidate behavior is not executed",
      "lane performs Node syntax validation only",
      "daemon spawn and localhost health request are not invoked"
    ]
  },
  "safety": {
    "executes_candidate_behavior": false,
    "writes_source_files": false,
    "uses_network": false,
    "requires_secrets": false,
    "notes": "This explicit lane checks readability/static signals and Node syntax only; it does not spawn the bridge daemon or call localhost."
  },
  "commands": [
    {
      "kind": "readability",
      "command": "internal: read scripts/bridge-only-smoke-test.js",
      "expected": "Candidate file exists and is readable within the repository."
    },
    {
      "kind": "static-safety",
      "command": "internal: scan destructive/network/credential/live signals",
      "expected": "Dangerous candidates remain rejected or require an explicit human-approved lane."
    },
    {
      "kind": "external-risk-coverage",
      "command": "internal: verify explicit read-only fixture coverage",
      "expected": "Lane does not execute daemon spawn, localhost request, network access, or credential access."
    },
    {
      "kind": "node-syntax",
      "command": "node --check scripts/bridge-only-smoke-test.js",
      "expected": "Node parser accepts the candidate without executing it."
    }
  ]
}
