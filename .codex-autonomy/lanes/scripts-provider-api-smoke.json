{
  "schema": "codex-autonomy.validation-lane.v1",
  "id": "scripts-provider-api-smoke",
  "candidate_id": "scripts/provider-api-smoke.js",
  "candidate_path": "scripts/provider-api-smoke.js",
  "created_at": "2026-06-04T13:59:22.925Z",
  "updated_at": "2026-06-04T13:59:22.925Z",
  "type": "explicit-safe-validation-lane",
  "description": "Mock-provider lane for provider API smoke script static validation; does not start local servers or perform provider calls.",
  "external_risk_coverage": {
    "approved": true,
    "strategy": "mock",
    "evidence": [
      "candidate behavior is not executed",
      "lane performs Node syntax validation only",
      "fixture HTTP providers in the candidate are not started"
    ]
  },
  "safety": {
    "executes_candidate_behavior": false,
    "writes_source_files": false,
    "uses_network": false,
    "requires_secrets": false,
    "notes": "This explicit lane checks readability/static signals and Node syntax only; it does not start fixture HTTP servers or use provider credentials."
  },
  "commands": [
    {
      "kind": "readability",
      "command": "internal: read scripts/provider-api-smoke.js",
      "expected": "Candidate file exists and is readable within the repository."
    },
    {
      "kind": "static-safety",
      "command": "internal: scan destructive/network/credential/live signals",
      "expected": "Dangerous candidates remain rejected or require an explicit human-approved lane."
    },
    {
      "kind": "external-risk-coverage",
      "command": "internal: verify explicit mock-provider coverage",
      "expected": "Lane does not start candidate fixture servers, call providers, use network, or read credentials."
    },
    {
      "kind": "node-syntax",
      "command": "node --check scripts/provider-api-smoke.js",
      "expected": "Node parser accepts the candidate without executing it."
    }
  ]
}
