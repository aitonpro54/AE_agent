{
  "schema": "codex-autonomy.validation-lane.v1",
  "id": "mcp-server-mcp-adapter",
  "candidate_id": "mcp-server/mcp-adapter.js",
  "candidate_path": "mcp-server/mcp-adapter.js",
  "created_at": "2026-06-04T13:59:22.963Z",
  "updated_at": "2026-06-04T13:59:22.963Z",
  "type": "explicit-safe-validation-lane",
  "description": "Read-only fixture lane for MCP adapter static validation; does not start the adapter or bridge daemon.",
  "external_risk_coverage": {
    "approved": true,
    "strategy": "read-only-fixture",
    "evidence": [
      "candidate behavior is not executed",
      "lane performs Node syntax validation only",
      "daemon auto-start, stdio loop, and localhost calls are not invoked"
    ]
  },
  "safety": {
    "executes_candidate_behavior": false,
    "writes_source_files": false,
    "uses_network": false,
    "requires_secrets": false,
    "notes": "This explicit lane checks readability/static signals and Node syntax only; it does not start the MCP adapter, bridge daemon, or localhost requests."
  },
  "commands": [
    {
      "kind": "readability",
      "command": "internal: read mcp-server/mcp-adapter.js",
      "expected": "Candidate file exists and is readable within the repository."
    },
    {
      "kind": "static-safety",
      "command": "internal: scan destructive/network/credential/live signals",
      "expected": "Dangerous candidates remain rejected or require an explicit human-approved lane."
    },
    {
      "kind": "external-risk-coverage",
      "command": "internal: verify explicit read-only fixture coverage",
      "expected": "Lane does not start the adapter, daemon, stdio loop, network access, or credential access."
    },
    {
      "kind": "node-syntax",
      "command": "node --check mcp-server/mcp-adapter.js",
      "expected": "Node parser accepts the candidate without executing it."
    }
  ]
}
